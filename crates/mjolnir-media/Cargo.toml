[package]
name = "mjolnir-media"
description = "Real-time media primitives (jitter buffering, mixing) for mesh streaming"
version.workspace = true
edition.workspace = true
license.workspace = true

[dependencies]
